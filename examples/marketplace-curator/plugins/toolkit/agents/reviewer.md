---
name: reviewer
description: Reviews a diff for correctness and style
---
# reviewer

The toolkit plugin's own agent. An agent links under its bare frontmatter name
(`reviewer`), not the plugin prefix (NS-40).
