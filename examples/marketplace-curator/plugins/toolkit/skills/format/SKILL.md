---
name: format
description: Format the working tree with the project formatter
---
# format

The toolkit plugin's own skill. It installs as `toolkit:format` (the plugin name
is the default namespace, MKT-5).
