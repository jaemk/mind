---
description: Run the test suite, then open a PR for the current branch.
---

# ship

Run the project's test suite. If it passes, push the current branch and open a
pull request against `main`. If it fails, stop and report the failure instead
of pushing.
