# scan notes

A bundled resource of the `scan` skill, addressed as `{{self}}/resources/notes.md`.
It ships and installs with the skill (the whole skill directory is one item), so
`{{self}}` keeps the reference correct even when a prefix renames the skill.
