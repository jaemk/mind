---
name: review
description: Review a diff for bugs; defers to a sibling rule for formatting.
---

# review

Read the diff and flag correctness issues. For formatting, follow the
{{ns:style}} rule rather than restating it here.

This file lives inside a skill directory: reference tokens are expanded in every
file of an item, not just at its top level, and an item may reference a sibling
of a different kind (here, a skill referencing a rule).
