## What changed

## Spec

- [ ] Behavior changes are documented in `spec/` with a stable ID, cited from
      a test (or allowlisted in `tests/spec_coverage.rs` with a reason).
- [ ] The feature-status row in `spec/README.md` is updated.

## Testing

- [ ] `make ci-local` passes.
