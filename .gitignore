/target
/.idea
/docs/book
# Local hermetic probe runner, recreated from the hermetic-verify skill. The
# committed contract is the allowlist entry and the CLAUDE.md note, not the body.
/scripts/probe.sh
/local/
# Machine-local Claude Code state: previously ignored only by the global
# gitignore and .git/info/exclude, neither of which a fresh clone inherits.
/.claude/settings.local.json
/.claude/worktrees/
/.claude/scheduled_tasks.lock
